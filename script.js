var article = document.querySelector( ".article" );

var btn = document.querySelectorAll( ".btn" );

var links = document.querySelector(".links")


function active() {
    article.classList.toggle( "activation" )
    links.classList.toggle("dplaynone")
}
